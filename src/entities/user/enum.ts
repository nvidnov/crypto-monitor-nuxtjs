export enum Role {
  ADMIN = "Admin",
  CURATOR = "Curator",
  USER = "User",
}
export enum IdRole {
  ADMIN = 1,
  CURATOR = 2,
  USER = 3,
}
export enum DescriptionRole {
  ADMIN = "Администратор",
  CURATOR = "Куратор",
  USER = "Пользователь",
}
